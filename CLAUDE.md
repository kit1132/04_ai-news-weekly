# AI News Weekly Digest

## 基本方針

- 出力は日本語
- 簡潔で実用的な情報を好む。冗長な説明は不要
- 定量的な根拠を重視
- AIエージェント・開発ツールのニュースに特化（kit1132/01_ai-news-master, 02_ai-news-copilot, 03_ai-news-industry の集約）

## プロジェクト構成

- `digests/YYYY/MM/ai-news-YYYY-MM-DD.md` — 週次ダイジェスト本体（YYYY-MM-DD はその週の日曜日付）
- `index.html` — ダイジェストの HTML ビューア（marked.js 使用、GitHub Pages で公開）
- `files.json` — ビューアが参照するダイジェストファイル一覧（新しい順、パスはルートからの相対）
- `.nojekyll` — GitHub Pages の Jekyll 処理を無効化

## 実行環境

- Claude Code on the web（クラウド実行・Routines）
- スケジュールタスクとして毎週日曜 09:00 JST に自動実行
- ネットワーク: GitHub MCP 経由で 3 入力リポを参照
- **ブランチ運用**: 週次デリバリは `claude/weekly-summary-{YYYY}-W{NN}` を `main` から派生、Draft PR として提出。`main` には直接 push しない
- **日付**: JST（Asia/Tokyo, UTC+9）で判定する。`TZ=Asia/Tokyo date +%Y-%m-%d` で当日日付、`TZ=Asia/Tokyo date +%G-W%V` で ISO 週番号を取得

## 入力（読み取り専用・3 リポ横断）

| リポ | リポタグ | パス |
|---|---|---|
| `kit1132/01_ai-news-master` | `[master]` | `digests/{YYYY}/{MM}/ai-news-YYYY-MM-DD.md` |
| `kit1132/02_ai-news-copilot` | `[copilot]` | `digests/{YYYY}/{MM}/ai-news-YYYY-MM-DD.md` |
| `kit1132/03_ai-news-industry` | `[industry]` | `digests/{YYYY}/{MM}/ai-news-YYYY-MM-DD.md` |

7 日窓 = `[今日-6日, 今日]` の連続日付。月をまたぐ場合は正しい `MM` ディレクトリを参照。取得失敗 / 404 は「欠損」として記録（リポタグ・日付）。

## 出力フォーマット

```
# AI News Weekly Digest - YYYY-MM-DD (日曜日) [週: YYYY-W{NN}]

> 対象期間: YYYY-MM-DD 〜 YYYY-MM-DD
> 入力: 取得成功 N / 21（欠損があれば [リポタグ] YYYY-MM-DD 形式で列挙）

## 今週のハイライト
（3 リポ横断のインパクト順 3〜5 件、<explanation_style> の 4 ステップ適用）

---

## {カテゴリ名}

### {見出し} — [master]
- 詳細
- https://...

### {見出し} — [copilot][industry]
- 重複統合した詳細
- https://...1, https://...2

---

## {次のカテゴリ名}
...

---

## 来週の注目予定
- 将来日付のイベントを統合

---

## 改善メモ
- 3 リポの「改善メモ」セクションを統合・重複排除
```

### カテゴリの扱い

- カテゴリ名は**固定しない**。3 リポの当週分 daily に出てきた `## {カテゴリ}` 見出しを集約し、自然に統合する
- 同一・類似カテゴリは統合（例: master と copilot で同名なら 1 つにまとめる）
- カテゴリ順は当週で件数の多い順、または読み手にとっての主要度順

### リポタグ

- `[master]` / `[copilot]` / `[industry]` の 3 種
- 各 `### 見出し` の**末尾**に付与（` — [master]` のように半角スペース＋em-dash＋スペース＋角括弧）
- 重複統合した項目は `[master][copilot]` のように複数タグを併記し、URL も併記

## 週次ダイジェスト生成手順

1. JST 当日日付（= 実行日の日曜）と ISO 週番号を取得
2. 出力先 `digests/{YYYY}/{MM}/ai-news-{YYYY-MM-DD}.md` が `main` 上に既存なら**生成中止し「既存ファイルあり: {パス}」と通知して終了**
3. 7 日分 × 3 リポ = 最大 21 ファイルを GitHub MCP で取得
4. 取得失敗ファイルは欠損として記録（リポタグ・日付）
5. **21 ファイル全欠損なら生成中止**（空ファイル / 空 PR を作らない）
6. ブランチ作成: `claude/weekly-summary-{YYYY}-W{NN}` を `main` から派生
7. 上記フォーマットに従い `digests/{YYYY}/{MM}/ai-news-{YYYY-MM-DD}.md` を生成
8. `files.json` の配列**先頭**に新ファイルパス（`digests/{YYYY}/{MM}/ai-news-{YYYY-MM-DD}.md`）を追加
9. ブランチに 2 ファイル（digest + files.json）をコミット
10. Draft PR を作成（タイトル `weekly: AI news summary {YYYY}-W{NN}`、ベース `main`、本文に対象期間・取得成功数・欠損リスト・自動生成である旨）

## 不変条件

- 入力 3 リポ（01〜03）の `digests/` 配下を**変更しない**（読み取りのみ）
- 既存週次ファイルを**上書きしない**
- `main` には直接 commit しない（必ず Draft PR 経由）

---

## プロンプトブロック（生成時に厳守）

<explanation_style>
## 適用範囲
この解説スタイルは、主に「今週のハイライト」セクションに適用する。
他セクション（カテゴリ別まとめ・来週の注目予定・改善メモ集約）は事実の整理を優先し、4ステップの厳密な適用は不要とする。
## 解説の構造
トピックの性質に応じて、以下のステップを適用してください。
1. 事実の提示：何が起きたのかを端的に述べる
2. 根拠の提示：定量データがあれば数字で、なければ技術的意義や業界への影響で裏付ける
3. 影響の具体化：このニュースが読者の日常・判断にどう波及するかを示す
4. 行動指針：「で、どうすればいいの？」に答える。明確な行動がない場合は「経過観察」と明示する
## 解説の原則
- 比較対象を置く：単体で評価せず、代替手段と並べて判断基準を渡す
- 判断の方向性を明確にする：「不要」なら切り捨て、「真似できない」なら代替行動へ誘導する。曖昧に濁さない
- 一貫したメッセージに収束させる：複数トピックを扱う場合、結論を一つの核心メッセージに集約し、異なる角度から補強する
- 情報量が増えても、読者がやるべきことはむしろ減るように構成する
- 感情や印象ではなく、根拠で議論する
- 「参考になります」「注目です」のような曖昧な締めは使わない
- 行動指針のない解説はしない
</explanation_style>

<workflow>
以下の手順で週次AIニュースサマリーを作成してください。
## 対象ファイル
- 各入力リポジトリの `digests/{YYYY}/{MM}/ai-news-YYYY-MM-DD.md` から今日から遡って直近7日分を読み取る
- 月をまたぐ場合も日付が範囲内なら含める
## 出力先
本リポジトリ kit1132/04_ai-news-weekly の `digests/{YYYY}/{MM}/ai-news-YYYY-MM-DD.md`（YYYY-MM-DD は実行日の日曜日付）
- 同名ファイルが既に存在する場合は上書きせずスキップし、「既存ファイルあり：{パス}」と通知する
## 出力構成
1. 今週のハイライト：全ソース横断でインパクト順に3〜5件。各項目に <explanation_style> の4ステップを適用する
2. カテゴリ別まとめ：日次ファイルの `##` 見出しを集約し、可変カテゴリで構成。各 `###` 見出し末尾にリポタグ（[master]/[copilot]/[industry]）を付与し、ソースURLを併記する
3. 来週の注目予定：将来日付のイベントを統合
4. 改善メモ集約：3 リポの「改善メモ」を重複排除して統合
## 制約
- 日次ファイル（入力 3 リポの digests/ 配下）は一切変更しない
- ソース間で重複するニュースは統合し、リポタグと URL を併記
- ISO 8601 準拠の週番号を使用
- 日本語で出力
</workflow>

<quality_check>
出力完了後、以下の3点で自己検証してください。
- YAGNI：読者の行動判断に不要な情報・補足が含まれていないか
- DRY：出力全体に不要な繰り返しがないか
- KISS：不必要に複雑な表現・構成になっていないか
</quality_check>
